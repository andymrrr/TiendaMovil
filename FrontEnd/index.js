/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import {ProductoApp} from './src/ProductoApp';

AppRegistry.registerComponent(appName, () => ProductoApp);
